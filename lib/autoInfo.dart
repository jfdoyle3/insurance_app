import 'package:flutter/material.dart';

class AutoInfo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Auto Container'),
    );
  }
}
